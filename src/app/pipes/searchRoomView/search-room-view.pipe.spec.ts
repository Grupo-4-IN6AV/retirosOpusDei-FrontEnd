import { SearchRoomViewPipe } from './search-room-view.pipe';

describe('SearchRoomViewPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchRoomViewPipe();
    expect(pipe).toBeTruthy();
  });
});
